/**
 * These are required to read and write JSON objects (locally).
 */
const os = require( 'os' );
const storage = require( 'electron-json-storage' );

/**
 * This function loads the user settings. If the configuration file cannot be
 * found, this function creates a default settings.json file.
 */
function loadSettings() {
    
}
